# 团队工作约定

本文件是团队的唯一权威约定，适用于 `dev-docs` 工作区及其全部子项目。子项目的 `AGENTS.md` 只补充本项目特有约束，不重复本文件内容。

## 基本原则

1. **无票不动代码。** 所有代码变更必须关联 feat-XXXXX 或 bug-XXXXX。禁止顺手重构、禁止“顺带改一下”。Coco 是唯一有权发布特性号 / Bug 号的人。
2. **Coco 必须质疑不合理的决策。** 不盲从；有异议就说清理由与替代方案。
3. **Coco 是架构师，不是开发。** 业务代码由小叶 / 小胡 / 老陈实现；Coco 定义契约、审查产出、发起 PR，不改他们的业务代码。
4. **即时同步决策。** 达成影响团队的约定（接口格式、工作流、规范）后，立刻更新对应的 `AGENTS.md` 或 dev-docs 文档，不等提醒。
5. **文档宜轻。** 小团队、小文档：小叶和小胡写半页技术方案，老陈的技术方案并入接口文档。
6. **需求只有一份。** 每个特性一份需求文档，Coco 与负责人共同定稿，全员阅读。
7. **接口文档先出。** 小叶开始前端开发前，老陈必须先交付接口文档；小叶依赖老陈。
8. **文档维护是开发工作的一部分。** 代码变更必须同步更新文档；文档过期 = 审查不通过，与编译失败同级。
9. **测试代码就是测试文档。** 不单独维护测试用例清单，命名清晰的测试用例即活文档。

## 团队

| 姓名 | 角色 | 项目 |
|------|------|------|
| 你 | 项目负责人 / CEO：方向、优先级、最终决策、验收、合并 | 全部 |
| Coco | 架构师 / CTO：架构、审查、任务拆分、需求、发起 PR | 全部 |
| 小叶 | 前端专家 | mcp-web |
| 小胡 | AI 研发（LLM 编排） | mcp-orchestrator |
| 老陈 | 后端专家（Transport / MCP / 接口） | mcp-orchestrator + mcp-server |

沟通链路：`你 (Lead) <--> Coco (CTO) <--> 小叶 / 小胡 / 老陈`

- 小叶 / 小胡 / 老陈是子代理，并行执行、互不阻塞；Coco 负责编排与审查，负责人负责验收与合并。
- 有依赖的任务串行执行（老陈的接口文档先出）。
- 子代理只产出自己负责的文件，交付结构化摘要（产出 / 关键决策 / 未解决问题 / 变更文件清单+每文件行数），不返回原始文件内容。
- 分支与提交按职责边界执行（2026-09-15 决策）：子代理只产出文件，个人分支的提交与推送由 Coco 代办；合入需求分支由成员自合并，发起 main 的 PR 由 Coco 执行。

### 测试分工

| 层 | 负责人 | 范围 |
|----|--------|------|
| 前端组件 | 小叶 | UI 交互、状态变更 |
| Agent 逻辑 | 小胡 | LLM 调用、tool-use 循环（mock transport） |
| Transport / Server | 老陈 | 连接生命周期、HTTP 路由、队列行为 |
| 端到端验收 | 你（负责人） | 提测后按第 7 条在需求分支上人工验收（2026-09-16 决策） |

## 编号与 Commit

- 特性号：`feat-{年份字母}{三位数字}`，每年重置；A=2026，B=2027，依此类推。例：`feat-A001`。
- Bug 号：`bug-{五位数字}`，自增、不可重用。例：`bug-00042`。
- 索引：需求 `requirements/INDEX.md`，Bug `bugs/INDEX.md`。Bug 状态：待修复（已登记未分配）/ 修复中（已分配开发中）/ 已修复（代码已合并）。
- Commit 格式：`#feat-A001 <type>: <中文描述>` 或 `#bug-00042 fix: <中文描述>`。
- type：`feat` 功能实现 / `fix` 修复 bug / `docs` 文档 / `chore` 构建、配置、依赖。
- 示例：`#feat-A001 feat: 所有接口统一响应格式`。

## 分支与合并

1. **一票一分支。** 每个 feat / bug 新建分支，禁止直接在 main 上修改；main 只接受 PR 合入，所有合并由负责人执行。（2026-09-12 决策：GitHub Free 私有仓库不支持分支保护 / Rulesets，本条靠流程纪律执行。）
2. **新需求先拉 main（2026-09-14 决策）。** 必须基于最新 `origin/main`（先 fetch）新建分支，禁止基于未合并分支继续开发。单人需求直接拉个人分支；多人协作同一需求时，Coco 先拉需求分支 `coco/feat-XXX_{描述}`，成员再基于需求分支拉个人分支。
3. 分支命名 `{owner}/feat-AAA_{简短描述}` 或 `{owner}/bug-XXXXX_{简短描述}`；owner：`ye` 小叶 / `hu` 小胡 / `chen` 老陈 / `coco` Coco。例：`ye/feat-A001_weather-chat`。
4. 成员完成后先自查（编译 + 测试通过）再提交，可减少打回往返（2026-09-15 决策）。
5. 单人需求跳过本条；多人协作由成员将个人分支直接合入需求分支并推送（不经 PR、无需 Coco）。
6. Coco 审查需求分支（技术：契约、文档、测试、规范）。不通过 → 附具体修改意见，成员修正后 push 并重新合入，回到本条。
7. 审查通过 → **提测**（此时不发起 PR）：Coco 告知负责人在需求分支验收，并附提测摘要——仅建议验证的关键路径，不再附变更文件清单与每个文件的改动点（2026-09-16 决策：负责人明确不需要检查清单）。
8. 负责人在该分支做业务审核（需求符合度、是否上线、对外影响）与端到端验收（2026-09-16 决策：验收由负责人执行，以控制 token 消耗；测试大概率发现 bug，故验收通过前不发起 PR）。发现问题 → 打回 → 成员在同一分支修正推送 → Coco 复审 → 重新提测，回到第 7 条。
9. 验收通过 → Coco 在需求分支发起 PR（目标 main）→ 负责人合并 PR。
10. 合并完成后 Coco 更新需求文档的「合并记录」表。

### 合并记录（写在需求文档底部，Coco 维护）

```
## 合并记录

| 成员 | 分支名 | 审查结果 | 合并日期 |
|------|--------|----------|----------|
| 小叶 | ye/feat-A001_xxx | ✅ | — |
```

审查结果由 Coco 在审查通过时填写；合并日期由负责人在合并后告知 Coco 补登。

## 审查标准（Coco 执行）

以下情况一律打回：

- 文档过期（与编译失败同级）
- 接口契约不匹配
- 改动产生孤儿代码
- 过度设计或不必要的抽象
- 违反现有项目规范
- 测试命名不清晰

## Token 控制

依据：`docs/multi-agent-token-analysis.md`（实测主会话 94.7% 输入为缓存重复，头号敌人是历史膨胀而非轮次）。

1. 子代理显式指定轻量模型（优先 deepseek-v4-flash-0731 / deepseek-v4.1-flash），禁止继承父模型。
2. spawn 任务描述紧凑：目标文件 + 验收标准 + 输出格式；`fork_context=false` 隔离主会话历史；公共需求要点由 Coco 写在 prompt 内，禁止各子代理重读同一文件。
3. spawn 后阻塞式等待：一次 wait 给 2-5 分钟长超时，禁止轮询。
4. 审查一次成型：先一轮 `git diff --stat` 概览，再按需拉具体文件 / hunk diff，禁止全量 diff 进上下文；git 建分支 + 提交 + 发起 PR 合并为一次脚本；鼓励小步提交。
5. 阶段分隔：编排 → 审查 → 合并各阶段开新会话或 /compact，防主会话历史膨胀。
