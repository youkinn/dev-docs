# bug-00002: 选中"风云三国"标签后模型未路由到题库工具

> Bug 号：bug-00002
> 状态：待修复
> 关联特性：feat-A003
> 涉及项目：mcp-orchestrator
> 日期：2026-09-15

## 复现步骤

1. mcp-web 选择"风云三国"标签 → "问题查询"服务
2. 输入：`三国"三绝"中的智绝指的是谁`
3. 观察：模型未调用 `sango_query`，走自由作答
4. 对比：输入 `风云三国 三国"三绝"中的智绝指的是谁` → 调用 `sango_query` → 返回"诸葛亮"

## 预期行为

选中"风云三国"标签后，三国相关提问应自动路由到题库，不需要用户手动加"风云三国"前缀。

## 根因分析

`agent.ts` 的 `UNIFIED_SYSTEM_PROMPT` 触发条件过严：

> 适用域：仅限风云三国游戏内的招募武将问答题；其他三国历史或常识问答不属于本能力，不要调用 sango_query。

模型判断"三国三绝中的智绝指的是谁"属于普通三国历史问答，不调用工具。只有显式包含"风云三国"关键词时才会命中。

而 feat-A003 将标签降级为纯前端 UX，请求体只有 `{ message }`，后端无法感知用户选择了"风云三国"标签。两条设计互相矛盾。

## 修复方向

方案 A（首选，零 API 变动）：放宽 `UNIFIED_SYSTEM_PROMPT` 触发条件——三国相关提问一律先调用 `sango_query`；题库命中则返回答案，未命中则返回固定话术"题库未收录该题"。

方案 B（备选，需前后端同步上线）：恢复标签信息透传（domain 或类似字段），让后端感知用户意图。

## 分配

- 负责人：小胡
- 涉及文件：`mcp-orchestrator/src/agent.ts` — `UNIFIED_SYSTEM_PROMPT`