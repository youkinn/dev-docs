# bug-00021：域锁定题库问句答成模型先验知识（提示词瘦身删掉「候选不相关 → 未收录」约束，200 成功返回 + citations 空）

> Bug 号：bug-00021
> 状态：已修复（2026-09-23 负责人验收通过：「刘备字什么」→「题库未收录该题，请换个问法」；代码 `003fbef` + 集成用例 `022ae3d` 在 `coco/feat-A011_prompt-slimming`，**未合并 main**）
> 关联特性：feat-A011（负责人 2026-09-23 提测验收中发现；**A011 引入** —— 仅 A011 分支复现，main 上旧提示词含该约束）
> 涉及项目：mcp-orchestrator（主因）、mcp-server（次因，被主因掩盖）
> 登记：Coco ／ 报告：负责人 ／ 登记日期：2026-09-23

## 现象

```
POST /api/chat  {"message":"玄德是谁的字","domain":"fengyunsanguo"}
→ 200 {"code":200,"data":{"answer":"刘备","citations":[]},"message":""}
```

traceId `a5232947-e012-4c9d-9dfe-5a76c7abb0c6`（`log_type=chat`、`status=success`、`response_code=200`）。

负责人判据：`fengyunsanguo_query` 出参不是「刘备」，答案由大模型给出 —— 但接口层面与题库答案**无法区分**：`status=success`、`citations=[]`，用户看不出这是模型先验知识。

## 链路核对（工具出参与模型输出不一致）

| 环节 | 读数 |
|------|------|
| 路由 | `domain=fengyunsanguo` → 域锁定快路径，正常命中并预调工具 |
| 工具 | `fengyunsanguo_query`，`args={"text":"玄德是谁的字"}`，`caller=server` / `stage=fastpath`，耗时 2 ms，`status=success` |
| 工具出参 | `1. 关公坐骑赤兔马原本是谁的坐骑？ → 吕布`（**与问题不相关**） |
| 生成轮 | 1 条 `llm_call_logs`：`stage=generation`、`prompt_tokens=109`、`completion_tokens=1`、`finish_reason=stop`、`response_summary=[{"type":"text","text":"刘备"}]` |
| 注入 | `request_summary` 第 3 段 system = 上述候选（候选确实进了上下文，模型仍无视） |

结论：路由与工具调用都正常，**答案不在候选内**，来自模型自身先验知识。

## 定性（口径以需求文档为准）

| 出处 | 口径 |
|------|------|
| `requirements/feat-A003-model-tool-routing.md:48` 验收 3 | 语义指向三国但题库未收录 → 固定回复「题库未收录该题，请换个问法」，**不使用题库外知识作答** |
| `requirements/feat-A011-prompt-slimming.md:141` | 题库域：命中只输出答案原文；**未命中**只回「题库未收录该题，请换个问法」 |

事实核对（题库 88 题）：含 16 道「XX 的字是什么」，**无「刘备的字」**（刘备相关 6 题均不问字）→ 本问句属「题库未收录」，正解是未收录话术。

即：**是缺陷**（违反 feat-A003 验收 3）。feat-A011 §141 的「未命中」包含「候选不相关」，实现把它窄化成「候选为空」一种情形 —— 属语义收窄。

## 根因

两层，主因在编排层（唯一能兜住全部情形的一环）；召回层是暴露条件，不是可修点。

1. **主因 · 编排层提示词收窄**（`mcp-orchestrator/src/agent.ts:123` `FENGYUNSANGUO_DOMAIN_PROMPT`）
   feat-A011 瘦身（提交 `ec4cfcb`）把旧版两条约束删掉，新版只写「候选为『未召回到任何候选题目』时只回复『题库未收录该题，请换个问法』」→ **只覆盖候选为空**。候选存在但不相关时，生成轮没有任何约束 → 自由作答。
   被删的旧版约束（main 上仍在，A011 分支已删）：`UNIFIED_SYSTEM_PROMPT` 能力二第 4 条「候选中没有含义对应的题目时（包括只是字面相似、含义不同的），只回复『题库未收录该题，请换个问法』」、第 5 条「禁止使用题库之外的知识作答、补充或改写答案」，以及分域兜底「三国题库域」条。
2. **次因 · 召回层无门限**（`mcp-server/fengyunsanguo/src/fengyunsanguo-service.ts:291` `candidates()`）
   纯字符 bigram 的 Dice 系数，`filter(score > 0)` 后取 topN，默认 `limit=1` → **永不给空候选**。本问句与赤兔马题共享 `是谁` / `谁的` 两个字组，dice 0.235 排第一。契约本意即「只负责捞候选，是否含义对应由 LLM 判定」。

## 复现数据（2026-09-23 本地实测，题库 88 题、同算法复算）

`service.candidates("玄德是谁的字", 3)` 的 top1 与 trace 出参一致。

| 输入 | top1 候选 | bigram dice | L3 余弦（阈值 0.9） | 是否真命中 |
|------|-----------|-------------|--------------------|-----------|
| 玄德是谁的字 | 关公坐骑赤兔马原本是谁的坐骑？ | 0.235 | 0.389 | 否（题库无刘备的字） |
| 刘备的字是什么？ | 吕布的字是什么？ | **0.667** | 0.716 | 否 |
| 张飞字什么 | 张飞的字是什么？ | 0.400 | 0.602 | 是 |
| 夏侯惇字什么 | 夏侯惇的字是什么？ | 0.500 | 0.701 | 是 |
| 关羽的字是什么 | 关羽的字是什么？ | 1.000 | 1.000 | 是 |
| 三英战吕布 | 吕布的字是什么？ | 0.200 | 0.277 | 否 |
| 今天天气怎么样 | 无候选 | — | 0.162 | — |

## 为什么不能靠召回门限 / 语义门限修（重要）

- 「玄德是谁的字」0.235 可被门限（如 0.3）拦掉 → 空候选 → 模型回「题库未收录该题」。**能修掉第一现场，但不彻底。**
- 「刘备的字是什么？」top1 = **0.667**（「吕布的字是什么？」），**高于真命中「张飞字什么」0.400** —— 问法同模板（题库有 16 道「XX 的字是什么」），任何安全门限都拦不住；此时模型仍会自由作答。
- 故：相关性判定必须归 LLM（契约本意），门限只能作半程加固，不能替代提示词修复。

### 补充读数（2026-09-23）：dice 与余弦的分数完全交叠

同一算法复算「真命中（换了说法）」与「不相关（题库未收录）」两组：

| 类别 | 输入 | top1 候选 | bigram dice | L3 余弦 |
|------|------|-----------|-------------|---------|
| 真命中 | 刘备靠什么过活 | 刘备最初是靠什么谋生的？ | 0.375 | 0.461 |
| 真命中 | 关公骑的马原来是谁的 | 关公坐骑赤兔马原本是谁的坐骑？ | 0.381 | 0.453 |
| 真命中 | 曹操的小名是啥 | 曹操的小名叫什么？ | 0.615 | 0.645 |
| 真命中 | 张飞字什么 | 张飞的字是什么？ | 0.400 | 0.602 |
| 真命中 | 夏侯惇字什么 | 夏侯惇的字是什么？ | 0.500 | 0.701 |
| 不相关 | 张飞以前是干什么的 | 张飞的字是什么？ | 0.286 | **0.626** |
| 不相关 | 刘备的字是什么？ | 吕布的字是什么？ | **0.667** | **0.716** |
| 不相关 | 玄德是谁的字 | 关公坐骑赤兔马原本是谁的坐骑？ | 0.235 | 0.389 |
| 不相关 | 三英战吕布 | 吕布的字是什么？ | 0.200 | 0.277 |

结论：**两类分数完全交叠** —— 不相关的余弦 0.626 / 0.716 高于真命中的 0.461 / 0.453 / 0.602。任何单一阈值（dice 或余弦）要么漏放不相关、要么误杀真命中，**不存在安全分界**。

根因不在阈值，而在判据本身：「玄德是谁的字」的正解依赖「刘备字玄德」这一**外部知识**，召回层只能看字面 / 向量相似，无法知道题库里没有「刘备的字」；唯一能判「候选中没有含义对应的题目」的是 LLM（feat-A002 分层本意：本地召回 Top-K + LLM 语义判定）。故**语义识别的正确位置是生成轮，不是召回层**。

### 老陈侧可选（均为加固，不能替代主修）

| 选项 | 内容 | 判断 |
|------|------|------|
| A. 候选带置信标注 | 出参附带 score（或置信档位），供 LLM 判定与日志排查参照；不改召回行为 | 低风险、零误杀，建议做（可并入 feat-A012 或新 feat） |
| B. 换真 embedding 召回 | feat-A002 已预留路径（题库上千后换 embedding 召回） | 提升的是**召回排序质量**，不是「收录 / 未收录」判定；需标定集，属独立立项 |
| C. 召回层硬语义门限 | 低于阈值即返回空候选 | **不做** —— 上表已证必然误杀真命中 |
| D. 召回层加一层小模型判定 | 把「是否含义对应」前移到召回层 | **不做** —— 职责重叠（L3 已有同类识别层，bug-00019「另议」已暴露其与分类轮重叠），且多付一次 LLM 调用 |

## 影响面

- 触发面：题库域生成轮（域锁定快路径 + auto 分类编号 2），凡「题库未收录、却被召回不相关候选」的问句。
- 严重度：**重大** —— 域锁定被绕过。用户拿到的是模型先验答案，却以 `200 success` + `citations=[]` 返回，与题库答案不可区分；本次「刘备」恰好正确属巧合，**错误答案同样会被当成功返回**。
- 仅 A011 分支复现：main 上旧提示词含约束，同问句会回未收录话术 → 属 A011 提测验收打回项。

## 修复口径（待负责人确认）

1. **主修 · 小胡**（mcp-orchestrator，一行提示词）：`FENGYUNSANGUO_DOMAIN_PROMPT` 补回「候选与问题含义不对应（含只是字面相似）时同样只回『题库未收录该题，请换个问法』，禁止用题库外知识作答」。
2. **可选加固 · 老陈**（mcp-server）：`candidates()` 低于门限不返回候选。需按上表标定阈值（本地打分，不涉 LLM 调用）；明确**不能替代主修**，是否做由负责人定。
3. **顺带核查（同一瘦身引入的同类收窄）**：feat-A011 §187 自己提到的 99 分支「无检索依据时不答三国内容」是否也已丢失；原著域提示词是否同样只剩单一分支。

## 修复实现（2026-09-23）

- 已按修复口径 1 实施（`mcp-orchestrator` 提交 `003fbef`）：`FENGYUNSANGUO_DOMAIN_PROMPT` 补回「候选为空、或候选中没有含义对应的题目（含只是字面相似、含义不同的）时，只回复『题库未收录该题，请换个问法』，禁止用题库外的知识作答、补充或改写答案」。
- 新增不变量断言「③a 题库域候选不相关（含义不对应 → 未收录）时不得用题库外知识作答」（`src/test/feat-A004/prompt-invariants.test.ts`）。
- Coco 复跑：`npm run build` 通过；`node --test build/test/feat-A004/prompt-invariants.test.js build/test/feat-A003/integration.test.js` → 23/23 通过。
- 补测试（提交 `022ae3d`）：`src/test/feat-A003/integration.test.ts:595` 新增「题库非空但候选与问题不相关 → 注入该候选且服务端原样返回未收录话术」，覆盖原先零用例的真实分支（原唯一用例走题库为空）。Coco 复跑：build 通过、integration + prompt-invariants 24/24 全绿。
- 未做：修复口径 2（召回门限，已论证拦不住模板化问句）；修复口径 3 已按同一方法扫描（原著域完整、99 分支属 feat-A011 §187 条件项，当前不补）。

## 验证口径（修复后）

- 负例：「玄德是谁的字」「刘备的字是什么？」→ 回复「题库未收录该题，请换个问法」。
- 正例不回归：「关羽的字是什么」→「云长」；「张飞字什么」→「益德」；auto 路径（不带 domain）同问句结果一致。
- 日志：不得再出现「候选非空、但 `answer` 不在候选内」的 `success` 记录。

## 复盘（2026-09-23 负责人确认）

漏因三层，主责在审查：

1. **实现读窄了需求**：需求里的「未命中」涵盖「候选为空 / 候选不相关」两种情形（A011 §141 与 feat-A003 验收 3 口径一致），实现只搬了「未收录话术」那句，丢了判定条件与禁令。
2. **审查没做「展开」**：只拿「未命中」这个词对照实现文案，未把概括词展开成可执行分支逐一验证；并把「测试全绿」当证据 —— 唯一覆盖「未收录」的集成用例（`src/test/feat-A003/integration.test.ts:576`）走的是**题库为空**（`questions: []`）这条**未坏**的分支，真实路径（有候选、但不相关）零用例。
3. **不变量测试是照录型**：只断言话术字面存在，删约束仍通过。

定性修正：需求文档「未命中」无歧义 → **不改 A011 §141**（需求没错，改即迁就实现）。

补漏：审查标准增「概括词 / 负例词展开成判定用例，每分支独立断言」（`AGENTS.md` 审查标准，2026-09-23）；「题库非空 + 不相关候选 → 未收录话术」的集成用例已补（`022ae3d`）。

顺带扫描（同一方法扫其余域）：原著域「片段中确实没有相关内容 → 演义中未涉及，禁止用先验知识补全」判定条件与禁令俱在、完整；99 分支无三国约束属 feat-A011 §187 的条件项，当前不补 → 本次「读窄」只此一处。

## 登记说明

根因跨两层（编排提示词 + 召回契约）、需承载复现读数与「为何不加门限」的论证，且涉及域锁定对外口径，故单独建文件跟踪（非「根因显然、改一两行」类小 bug）。