1. 目前在前厅 /weather页面的聊天框中，勾选 “三国演义”开始问问题。接口响应，偏慢，普遍在10s左右，有时候还有“请求超时，请稍后重试。”
   - 2026-09-16 已修复（代码层）：domain=sango-novel 快路径单次 LLM + 注入收窄（top3 + 检索词附近窗口）；引用校验本地化 0 次 LLM。命中回答只输出最符合一段。orchestrator `6105cc3` / `9da8f0a`。
   - 剩余耗时 7~17s 依赖火山方舟缓存显式开通（负责人处理中）→ 目标 1~2s。
2. （2026-09-16 18:08 新增）sango-novel 检索命中回答固定格式：先结论，后「引用原文」（出处：第X回 回目），出处只到回目。例：夏侯惇字元让。「又有沛国谯人夏侯惇，字元让」（出处：第5回 发矫诏诸镇应曹公 破关兵三英战吕布）
   - 2026-09-16 已修复：prompt 能力三第 6 条固定格式 + applyNovelCitationGuard 服务端强制（格式不符 → 兜底本身即固定格式）。orchestrator `6105cc3`。