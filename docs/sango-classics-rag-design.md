# 三国演义解读 · 技术方案（设计稿）

> 状态：**设计稿**（待负责人讨论定稿 → 再立项开发）
> 编号：BL-016（立项后发 feat 号，拟 `feat-A004`）
> 配套：决策 / 探针方案 / 结果见 `sango-classics-rag-research.md`（§3 决策、§5 探针方案、§12 探针 A 结果）
> 范围：后端（老陈，mcp-server / mcp-orchestrator）+ AI 编排（小胡，mcp-orchestrator）。
> 前端（小叶）：等老陈接口文档出完再接入，本次无新技术，本文不展开。

## 1. 目标与边界

- 场景：三国迷问演义情节，只给「原文怎么说」。独立标签，标签内只讨论演义。
- 拒答是验收核心：原文没有的内容必须答「演义中未涉及」，禁止用先验补全。
- 非目标：不做演义 / 正史对比、不评价对错、不做文学批评；最小版本不做 query 改写、不做 rerank、不接专用向量数据库。

## 2. 架构与数据流（D1 定案：检索 = MCP 工具）

- 检索层落点 = **mcp-server 的 MCP 工具**（D1 决策 B，2026-09-16 Coco 需求变更），非 orchestrator 本地工具。
- **单入参定上下文**：检索工具只收一个「上下文」入参（source / domain），系统据此确定本次会话上下文（source=sanguo-yanyi → 注入领域提示词），大模型调度 MCP 工具。
- 数据流：
  `用户问句 → orchestrator 大模型 → 调 MCP 工具(source, query, limit) → mcp-server 本地检索（向量 + BM25 hybrid，只读离线向量）→ 返回「原文段落 + 出处（回目 / 段号）」→ 大模型按领域提示词归纳 → 引用硬校验 → 通过出答案 / 不通过兜底原文直出`
- 多一层 stdio 延迟与调试成本是 D1 已接受的代价；换来二期接《三国志》只换入参与语料、不动编排骨架。

## 3. 契约（L6）

- 工具 `sango_novel_search`（MCP）：
  - 入参：`source`（enum，本期 `sanguo-yanyi`，唯一上下文入参，D4 预留二期 `sanguozhi`）、`query`（白话问句）、`limit`（默认 5）
  - 出参：文本块列表，条目格式 `【出处】第N回 <回目> · 段M（类型）\n<原文段落>`，按相关度降序；无命中返回「未召回到任何原文段落」
- 会话上下文：`source` 单值决定领域提示词（§5 规则集），提示词由系统注入，不靠用户提供。
- 工具隔离（D3）：与 `sango_query` 并存为独立工具——「本标签只讨论演义」是产品承诺，靠工具隔离保证，不靠 prompt 祈祷。

## 4. 检索实现

- 语料：毛本 120 回全本（现状仅第五回已建，见 §6）；段级切分 + 段落类型（narration / verse / comment）。
- 索引：**BM25 + 向量 hybrid**。向量是离线产物：D2 本地 embedding、D5 Python 侧车只在构建期出向量、线上只读；BGE-M3 下载走 `hf-mirror.com`，实施前设 `HF_ENDPOINT`（见研究 §11）。
- 别名表：封闭集合（关羽 / 关云长 / 关公 / 汉寿亭侯…），属数据整理非 NLP；兜住 H3 字面层人名 / 地名差异。
- chunk 粒度：段级（H4 待验）；召回片段须自足，模型只归纳不补全。

## 5. 生成约束（探针 A 已验证的三件套）

**prompt 限定 ②（2026-09-16 复测通过版，规则原文）**：

1. 回答前必须先调用 `sango_novel_search` 工具检索《三国演义》原文。
2. 只依据工具返回的原文作答：人物、情节、数字都必须能在原文里找到。
3. 人名一律以召回原文为准：原文写谁就是谁，不得按常识 / 记忆替换、不得补别名、不得解释成别人。
4. 原文里没有相关内容时，只回复「演义中未涉及」，不得用其他知识补全。
5. 不评论、不纠正、不对比：不得说原文写错，不得提正史 / 影视 / 游戏，不得出现「实际是…」这类转折。

**引用硬校验**：答案中出现的断言人名必须能在召回原文中找到，否则判定「引用不成立」。

**兜底 ③（产品降级路径）**：引用校验不过或未检索 → 不归纳，直接「原文片段 + 出处」直出；把忠实度从生成层挪到检索层。探针实测：A2 四连中（3 次自然跟随、1 次兜底兜住），A3 拒答不变，工具调用率 7/7，生死线解除。

## 6. 语料工程（老陈）

- 全本 120 回（T1b）：公版毛本文字，准入标准见研究 §10（不得用带现代点校 / 注释 / 译文的排印本）。
- 现状：第五回已建 `data/sango-novel/corpus/chapter-005.json`（负责人提供 txt 已采用）。
- ⚠️ 待确认：全本来源（§11 实测 wikisource / GitHub / ctext 均不可达，仅剩负责人电子版路线）；底本 / 点校版权口径（正文公版 ≠ 现代点校排印本公版），**正式上线前必须确认**。

## 7. 交付物与依赖

| 交付物 | 负责人 | 依赖 |
|---|---|---|
| MCP 检索工具（mcp-server）+ **接口文档先行** | 老陈 | — |
| 全本语料清洗 / 索引 / 离线向量 | 老陈 | 全本来源（§6） |
| prompt 限定 + 引用校验 / 兜底逻辑落地（orchestrator） | 小胡 | 检索工具就绪 |
| 前端标签页 | 小叶 | 接口文档（老陈先出） |

依赖链：老陈接口文档 → 小叶；检索工具 → 小胡编排联调。文档过期 = 审查不通过。

## 8. 明确不做

query 改写、rerank、专用向量数据库（千级 chunk 用内存余弦 / sqlite-vec 足够）、二期《三国志》、fine-tune（BL-030 搁置）。

## 9. 待讨论问题

1. **全本语料来源**：120 回全本从哪来？§11 实测外部语料源全不可达，是否由负责人提供电子版？（阻塞语料工程，其余可并行）
2. **上下文入参命名**：D1 写「source / domain」——契约字段定名 `source`（enum）还是 `domain`？（我倾向 `source`：与 D4 预留维度一致，语义是数据源）
3. **兜底形态**：产品层兜底是否补一句结论归纳（如「按原文，斩华雄者系张三」）？探针层刻意不归纳。
4. **引用校验强度**：只校验断言人名（别名表封闭集合），还是全量人名 NER？
5. **H1 前置**：探针 B（召回质量）仍未跑；全本语料建成后是否先验 H1 再开发？兜底已不依赖 H2，但仍依赖 H1 可用。