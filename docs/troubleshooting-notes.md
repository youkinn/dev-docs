# 排查笔记（基线 / 缺口 / 线索）

> 本文件是**笔记**，什么都可以往里面记：实测数据、经验阈值、采集缺口、零散线索、待验证猜想。
> 流程本身在 `docs/troubleshooting.md`（那份只放确定的东西，不轻易改）；本文件随时追加、随时推翻。
> 写法：日期 + 一句结论 + 依据；不确定的明确标「未验证」。

## 1. 已排查 trace 索引

| 日期 | traceId | 现象 | 结论（根因层 + 判据） | 关联票 | 状态 |
|------|---------|------|----------------------|--------|------|
| 2026-09-22 | `f21dd6be-27c1-45dc-aa84-7da74e33d78a` | 手册实测样本（演义快路径） | 正常：1 轮 generation，`tool` 63 ms，`injected` 6 / `cited` 2 | — | 已定性 |
| 2026-09-22 | 批量 20+ 条 `success` 样本 | 采基线读数 | 演义快路径 `server` 1.3–2.0 s；题库域 ~1.1 s；auto 路由 ~3.0 s | — | 已定性（见「基线读数」） |
| 2026-09-23 | `a5232947-e012-4c9d-9dfe-5a76c7abb0c6` | 题库域问「玄德是谁的字」答「刘备」、`citations` 空 | 编排层：feat-A011 提示词瘦身删掉旧版「候选不相关 → 只回未收录 + 禁用题库外知识」约束（main 上旧版有、A011 分支无）；工具层：`fengyunsanguo_query` 无门限，实测 `candidates("玄德是谁的字",3)` top1 =「关公坐骑赤兔马原本是谁的坐骑？」，题库 88 题无「刘备的字」→ 正解应为「题库未收录该题，请换个问法」。召回读数（本地实测 dice）：本问句 top1=0.235，真命中「张飞字什么」0.400、「夏侯惇字什么」0.500，而模板化问句「刘备的字是什么？」top1=0.667（吕布的字是什么？）→ 门限只能拦「玄德是谁的字」这类，拦不住模板化问句 | bug-00021 | 待修复 |
| 2026-09-23 | `9b2825f3-53b6-4425-b812-38c7ff6066bf` | 题库域问「关于字什么」答「吕布的字是奉先。」 | 召回层：与 16 道「XX的字是什么」只共享「什么」→ dice 全并列 0.200，稳定排序取题库序 #2 吕布；快路径 limit 默认 1 只给一条候选；生成轮未守「含义不对应 → 未收录」与「只输出答案原文」 | bug-00022 | 待修复 |
| 2026-09-23 | `223eb61e-5f69-4f02-a61c-6d2e2fe3c14a` | 字序打乱句「运输工具骑着诸葛亮的是发明」答「木牛流马」 | 句子不通顺、无自洽语义、非问句 → 需求未定义此类口径。答对靠 bigram 对字序不敏感（dice 0.545，次名 0.160），**非语义理解**；与 bug-00022 同源（都由字面重合决定） | bug-00022（对照） | 口径未定义 |
| 2026-09-24 | `4f420544-529a-4643-ac0f-6629656d85cd` | 问「三英战吕布」answer 第 2 条引语来自第 19 回（陈登间吕布），与问题无关 | 召回排序：ch19:c0004 靠词面「吕布」+ label「陈登间吕布」加成 finalScore 0.77 登顶，压过真相关 ch5（0.768）；注入 top6 全量、模型引用 `[Q3]`、Guard 人物集合校验放行（不校验引语↔问题相关性） | bug-00005 | 待修复 |
| 2026-09-23 | `fa657317-74e0-4de5-9875-145cebebd6fb` | 「董卓通过貂蝉使用什么计策离间吕布与王允？」答「美人计」 | **错误前提**（原著为王允用计离间吕布与董卓，施动者与对象都反了）：字面 0.857 命中原题后照答答案，未纠正前提 → 期望行为需求未定义（口径未定，非正例） | bug-00022（标定样本） | 口径未定义 |
| 2026-09-23 | `aba8b6fe-ee87-4e92-b8e7-3098a735c6c5` | 域锁定演义，病句「武器的名字叫什么关羽」答「关羽的武器叫青龙刀。¹」并引用 ch27 挑锦袍原文 | **读数正常，非缺陷**：链路无「歧义/病句/低置信」判定点——fastpath 检索 query 原样直传（无改写、无门限），lexical 1572 → merged 1573 → `topN=10` → `injected=7` → `cited=1`；生成轮 1 轮（prompt 1981 / completion 10），拒答只由「片段中确实无内容」触发，无形态类拒答分支 | — | 已定性 |
| 2026-09-23 | `1a5fe278-2e02-45a8-84a5-5b2d4e599256` | 同一病句走 auto 路由（实测探针，真实 LLM 调用）答对 | auto 链路同样无低置信拒答：L3 `fengyunsanguo_quiz_route` 未命中（只做题库正向高置信识别，不做反向推定）→ 分类轮（prompt 68 / completion 1）输出 `1`（原著域）→ classify 预调 `sango_novel_search` → 生成轮答对并引用 ch27 | — | 已定性 |
| 2026-09-23 | `7269df08-1115-48ca-b6a9-f2f16fe8adca` | 演义域问「严颜义释张飞」按「张飞义释严颜」作答（答「严颜感张飞恩义乃降，即义释严颜之事」+ ch63 引用） | 主宾反转 / 错误前提未识别：lexical 词袋 + bge-m3 cosine（0.630）+ label「义释严颜」对反转均无判别力 → rank1 finalScore 0.836 高置信注入并引用；生成轮无前提校验照答。按 A004 契约「原文没有的答未涉及」应拒答 → 缺陷（同族 bug-00022，不同域不并票） | bug-00023 | 待修复 |
| 2026-09-24 | `138f04cf-de4d-4966-98bf-34a159d439d0` | 演义域「马超投靠刘备后，后来如何了」答「成为蜀汉五虎上将之一，最终病逝。¹」 | 生成轮先验作答：注入 6/10 段全在 ch57/58/65（衣带诏/兴兵/葭萌关），无一处讲结局；答案支撑段 ch73 五虎将 rank7=0.723 未注入、ch91 病故未进候选；引用 ch57 与结论无关。提示词第 3 条含「禁止先验补全」但模型不服从 → 缺陷 | bug-00028 | 待修复 |
| 2026-09-24 | `226899d4-a3cc-405f-8a87-c78b83c8e6b3` | 演义域「夏侯渊字什么」答「夏侯渊字妙才。¹」 | 数据事实：全语料无「字妙才」（ch71「妙才」系才略义）；alias 归一化把问句改成「妙才字什么」反拉偏；引用 ch5 只含夏侯惇字元让。契约（A004 拒答红线）下应回「演义中未涉及」，模型凭史实先验作答 → 缺陷 | bug-00028 | 待修复 |
| 2026-09-26 | `c549084b-fe65-46c2-8d19-a3029b0d2502` | A016 验收：问「孙权遣人向关羽求亲，关羽是怎么回复使者的」答「演义中未涉及」 | 护栏误裁：原始输出（responseSummary 实证）「关羽怒斥使者，拒绝联姻。[片段5]」正确、指针=0073:c0008 求亲被拒原文、人物校验过；句-片段重叠门（0.5 @ 2-gram）对改述结论字面重叠≈0 → 整句裁剪 → 无留存句拒答。检索侧 funnel injected=7 正常 | bug-00032 | 已定性 |
| 2026-09-26 | `ca7d9c6a-0f4e-4844-8a42-6cf1ca250e70` | A016 验收：问「孙刘联军大破曹操的战役是哪一场」答「演义中未涉及」 | 护栏误裁：原始输出「孙刘联军大破曹操的战役是赤壁之战。[片段2]」正确、指针=0077:c0009（含「破曹操于赤壁」）；重叠 0.2 < 0.5 → 裁剪拒答。次级暴露：ch49 火攻叙述 0049:c0018-20 未进 top20 候选，引用只能落间接提及段 | bug-00033 | 已定性 |
| 2026-09-26 | `67119582-e906-47ed-842b-a63b3381aa66` | A016 验收对照：问「夏侯惇的眼睛是怎么瞎的」答「夏侯惇左目为曹性射瞎」¹ | 对照组（护栏放行）：原始输出「夏侯惇左目为曹性射瞎。[片段1]」，与 0018:c0014 重叠 5/9≈0.556 过线 → 放行。与 275551c3 检索 / funnel / prompt tokens（2238）完全一致，仅措辞不同决定过门与否 | bug-00034（对照） | 已定性 |
| 2026-09-26 | `275551c3-a941-4506-bfdb-bc0a65fd00e4` | A016 验收：同题跨请求「夏侯惇的眼睛是怎么瞎的」答「演义中未涉」 | 护栏误裁：原始输出「夏侯惇的左眼被曹性射瞎。[片段1]」正确，但「左眼/的」措辞使 2-gram 重叠仅 3/10=0.3 < 0.5 → 裁剪拒答（同溯源 67119582 答对，恰为 A016 左目/左眼归一化范畴） | bug-00034 | 已定性 |
| 2026-09-26 | `c1246d50-0804-423d-a33a-43bcf9a4dba7` / `734b3989-1f5f-4e56-bfd4-4deab390296f` / `fb616910-29e4-459a-897c-74c735d95ed7` | 日志库再出测试噪声：「答案」（failed/500）/「妙才」（答错）/「元让」（答对）三连发 | 非自动脚本触发：package.json 无 test/pretest、无 husky/.githooks/.github，例行「改完跑全量测试」（README）每次手动执行即灌一批。读数：log_type=quiz、llm=0、tool=0、client 时间戳空、毫秒级连排（04:38:02.7–.8Z，与「随机一题/你好/夏侯惇的字是什么」同批）。根因=bug-00012 修复（chen/bug-00012_harness-in-memory b061ccd，两处注入 :memory:）已写好但未合入 main → 任何未含修复的分支跑全量测试都复发；全仓 14 个 createServer 测试文件仅 A003 两处未注入，修复覆盖完整 | bug-00012 | 已定性 |
| 2026-09-26 | `47201c42-4dc1-4867-9249-836a69d77107` | 问「夏侯惇的眼睛是怎么瞎的」：证据充分仍触发第 2 轮复核 + 复核轮耗时 6.3s（server 8.07s） | ①复核触发属设计内：答案是对片段（曹性射中左目→急用手拔箭→连眼珠拨出）的整句改述，实测 `sentenceFragmentOverlap=0.321 < 0.5`（阈值 0.5 @ 2-gram）→ 例行进边界语义复核，judge supported 放行（bug-00032/33/34 路径，与 bug-00034 同源，行为正确）；②6.3s=模型侧单次抖动：同 stage 近 18 次平均 1.36s、其余全部 0.7~1.5s，本条唯一 6× 离群；judge prompt 441 / out 1 / finish=stop / 无重试 / queueWait=0，与载荷、重试、排队无关 | — | 已定性 |
| 2026-09-26 | `96bbe8ae-9636-44d4-a56d-c5a6aaaf844b` / `f8aa23ba-e29b-45aa-9b37-1f68b8b045b9` / `4f04f5f3-01e9-4b8c-ba07-7a3360a14d93` | A016 提测（test-1921）：问「桃园结义」答泛泛「刘、关、张三人桃园结义，誓同生死」未点名三人、引用全为追述句；「桃园三结义」答对；「桃园之盟」同「桃园结义」 | 召回排序 + 归一化双因：①结拜标准段 `0001:c0010` 词面无事件短语，追述句（昭 0077 / 张飞 0015 / 鲁肃 0066「与刘备桃园结义之时」等）取其词面 →「桃园结义」trace 标准段 rank 8、注入窗（7）外；②「桃园三结义」被 `三结义` 键损坏改写为 `桃园桃园结义`（双桃园 tf 侥幸把标准段顶到 #1，即「答对」来源，bug-00036 残留）。修复＝canonical 规范为 桃园三结义（`三结义` 依判据一并移出改写键，normVersion a025ebb6）+ `0001:c0010` 典故标签 `story.json`「桃园三结义」（负责人定案按典故口径：个人结拜无势力主体，不按结盟/外交事件；口径见 tags-prompt.md）；本地重放三写法标准段全部 #1（随 bug-00036 修复） | bug-00036 | 已修复（随本票） |

> 历史排查（A004~A011 期间）散落在各 bug 文档，不回溯补登。

## 2. 基线读数（实测，供对照）

采样环境（2026-09-22）：`model=deepseek-v4-1-flash-260901`、`corpusChunks=2344`、`vectorScheme=bge-m3`；样本取自列表近 40 条中的 `success` + `chat` 记录，已剔除 bug-00012 噪声行（`domain` 空 + 无耗时）。

| 场景 | `queueWait` | `server` | `llm` | `tool` | LLM 轮次 · stage | `tokens` in/out | 工具 stage |
|------|------------|----------|-------|--------|------------------|-----------------|-----------|
| 演义域快路径（选「三国演义」） | 0–2 ms | 1.3–2.0 s | 1.28–1.91 s | 60–95 ms | 1 · generation | 1.8k–2.1k / 38–230 | fastpath:sango_novel_search |
| 题库域（选「风云三国」） | 0 ms | ~1.1 s | ~1.1 s | ~7 ms | 1 · generation | ~107 / ~2 | fastpath:fengyunsanguo_query |
| auto 路由（未选标签） | 0–2 ms | ~3.0 s | ~2.9 s | 3–68 ms | 2 · classify + generation | ~2.2k / 83–157 | l3:fengyunsanguo_quiz_route + classify:sango_novel_search |
| 异常样本：演义检索变慢 | 1 ms | 6.1 s | 2.8 s | **3.2 s** | 2 · generation ×2 | ~2.2k / 53 | fastpath:sango_novel_search |

参考（2026-09-22 全天 token-stats，按天聚合、日界 Asia/Shanghai）：输入 93615 / 输出 11312。

## 3. 阈值假设（**未验证**，我自己定的经验值）

用途：判「是否异常」时先有个抓手；被实测推翻就改，验证充分后再考虑固化进手册。

| 读数 | 假设阈值 | 含义 | 状态 |
|------|----------|------|------|
| `queueWait` | > 1 s | 排队异常（并发 / 前序请求卡住） | 未验证 |
| `tool` | > 1 s | MCP 侧异常（基线 60–100 ms） | 未验证 |
| 单轮 `llm` | > 8 s | 模型侧异常或截断重试 | 未验证 |
| `server − queueWait − llm − tool` | > 500 ms | 总台自身开销异常（拼接 / 序列化） | 未验证 |
| `cachedTokens / promptTokens` | < 0.5 | 提示词缓存失效（前缀变动） | 未验证 |
| `finishReason` | `length` | 输出被 `max_tokens` 截断（思考吃满 → 空答案，bug-00018） | 依据充分 |
| 同 `stage` 的 `seq` 条数 | > 1 | 发生重试 | 依据充分 |
| `nextRank.gapToTopN` | < 0.001 | 排序脆弱（第 N+1 名几乎同分） | 未验证 |
| `funnel.injected` vs `topN` | 差值 ≥ 1 | 注入视图截断 | 依据充分（A009 口径） |
| `env.degradedBm25Only` | `true` | 向量路降级（召回质量下降的环境根因） | 依据充分 |

## 4. 日志采集缺口（卡点）

登记判据（满足任一即记）：① 需要的数据日志里没有；② 有数据但要人肉翻进程日志 / 读代码；③ 结论只能靠推理、无法用读数证明。

| 编号 | 卡点（排查时遇到什么） | 需要什么数据 | 现状 | 影响 | 建议动作 | 状态 |
|------|----------------------|--------------|------|------|----------|------|
| GAP-01 | 老 trace 复现不了，无法确认当时跑的是哪版代码 | 请求级代码版本 / 构建指纹（git commit） | 无（只有 `llm_call_logs.model`） | 跨版本追溯失真，只能「重跑一遍看现在还复不复现」 | 主表加 `build`（启动时读 commit / 构建时间） | 待评估 |
| GAP-02 | chunkId 对不上、召回结果无法回溯 | 语料版本标识 | 仅有诊断 `env.corpusChunks` / `vectorScheme` 近似 | 语料重建后老 trace 的 chunkId 可能失效 | 语料构建落版本号（chunk 数 + 构建时间 + 内容哈希）随诊断回传 | 待评估 |
| GAP-03 | 空答案 / 答一半（bug-00018） | 思考内容 `reasoning_content` | 未落库；A012 只补 `reasoning_tokens` | 「思考吃满预算」只能靠 token 计数推断 | A012 定稿时评估是否落思考文本（或仅落长度 + 头尾摘要） | 待评估 |
| GAP-04 | 「注入视图不可见」（bug-00006） | 注入视图文本形态：片段顺序、每段字数、预算取舍 | 只有 `candidates[].injected` 布尔 + `funnel.injected` 计数 | 截断发生在哪一级只能推理，无法用读数证明 | 收尾落「注入清单」（chunkId 顺序 + 每段字数 + 被裁项与原因） | 待评估 |
| GAP-05 | 500 报错定位 | 错误栈 / 错误类型 | 主表只存 `errorMessage` 文本 | 编排异常要人肉翻进程 stdout | 主表加 `error_stack`（8000 截断）或失败时落错误明细 | 待评估 |
| GAP-06 | 判断「进程崩溃 / 重启」（骨架 `请求中断未完成回填`） | 进程生命周期事件（启动 / 退出 / 崩溃） | `console.*` 只到终端 stdout，不落盘 | 无法把异常 trace 与重启时刻对齐；重启即丢 | 总台输出重定向到文件（按天轮转）+ 启动落一行「版本 + 时间」 | 待评估 |
| GAP-07 | 随机一题（`quiz`）会话连续性 | `sessionId` | 请求体里有，主表不存 | 同一会话多条记录无法关联，判题状态机问题断链 | 主表加 `session_id`（零契约变更，字段已在请求体） | 待评估 |
| GAP-08 | 慢请求归因到排队后卡住 | 队列上下文：被谁挡住（前序 traceId）、队列积压 | 只有 `queueWait` 数值 | 并发下无法定位是哪个请求阻塞 | 出队时落「前序 traceId + 等待时长」或队列快照 | 待评估 |
| GAP-09 | 8000 截断后无法判断目标内容是否在结果里 | 截断前原始长度 + 关键指纹（如 chunkId 集合） | 只有「已截断」标记 | 长出参场景无法证明「召回了 / 没召回」 | 截断时同时落 `original_length` + 出参内 chunkId 列表 | 待评估 |
| GAP-10 | 重试率统计 / 空答案排查 | 重试标识（第几次调用） | 靠 `seq` 递增推断 | 统计与归因都要人工推断 | A012 已规划 `attempt` 字段 | 已有归属（A012） |
| GAP-11 | 前端侧异常（页面报错 / 请求没发出去） | 前端异常与请求失败记录 | 无（请求未达服务端时没有任何 trace） | UI 类问题必须真浏览器 + 人工观察（第 11 条） | 评估前端错误上报（低优先级，内网口径） | 待评估 |

## 5. 已解决的卡点（留痕，避免重复提）

| 编号 | 卡点 | 解决方式 |
|------|------|----------|
| GAP-12 | 排查结论散落、同一 trace 重复排查 | 本文件「已排查 trace 索引」 |
| GAP-13 | 判「快 / 慢」缺参考值 | 本文件「基线读数」 |
| GAP-14 | 看不出「谁发起这次工具调用」（trace `2ef3608a` 被读成模型乱调工具） | bug-00019：`tool_call_logs` 增 `caller` / `stage`（2026-09-22 已修复） |
| GAP-15 | 工具出参被诊断挤占截断（出参尾部丢失） | feat-A009 硬约束：落库前剥离 `_meta.diagnostics` |

## 6. 现象 → 疑似根因（参照，**可能过期**）

> bug 修过、实现改过之后，下表未必还成立；**判缺陷以需求文档为准**（见手册「怎么判」）。

| 现象 | 先看 | 读数特征 | 疑似根因（参照） | 记录日期 |
|------|------|----------|------------------|----------|
| 原文有答案却答「演义中未涉及」 | `funnel` + `candidates` | 目标 chunk 不在 `candidates` / `lexicalHits` 偏少 | 召回质量（bug-00003） | 2026-09-22 |
| 召回对但答案没用到 | `funnel.injected` / `cited` | `injected < topN` 或 `cited < injected` | 注入截断（bug-00006）/ 引用通道（bug-00005） | 2026-09-22 |
| 引用原文与结论不相关 | `candidates[].cited` + 工具出参 | 引用片段 `cited=false` | bug-00005 | 2026-09-22 |
| 问「第几回」答不出 | 工具入参 `query` + 诊断 `query.normalized` | 回号被剥离 | bug-00004 | 2026-09-22 |
| 空答案 / 答一半 | LLM `finishReason` + `completionTokens` | `length`，思考 token 吃满 | bug-00018（已修复） | 2026-09-22 |
| 选中标签后路由错 | `domain` + 工具 `stage` | 与预期域不同 | bug-00002 | 2026-09-22 |
| 域锁定问答答成模型先验知识（citations 空） | LLM `responseSummary` + 工具 `resultSummary` | 候选与问题不相关，模型 `completionTokens` 极小且输出不在候选内 | bug-00021 | 2026-09-23 |

## 7. 已知问题参照（**可能过期**）

> 权威清单是 `bugs/INDEX.md`（含状态与修复日期）；本表只是我排查时的快照，**不当依据**。

| Bug | 现象 | 状态（2026-09-22 快照） |
|-----|------|------------------------|
| bug-00002 | 选中「风云三国」标签后未路由到题库工具 | 待修复 |
| bug-00003 | 演义召回质量不达标（原文有答案却答「未涉及」） | 待修复 |
| bug-00004 | 问「第几回」类问句无法作答 | 待修复 |
| bug-00005 | 「引用原文」与结论不相关 / 无信息量 | 待修复 |
| bug-00006 | 答案已召回但注入视图不可见 | 待修复 |
| bug-00007 | 评测集质量缺陷 | 待修复（dev-docs） |
| bug-00008 | 召回评测口径缺陷 | 待修复（dev-docs） |
| bug-00012 | 测试 harness 写开发库 → 日志页测试噪声 | 待修复 |

## 8. 零散线索 / 待验证猜想

| 日期 | 线索 | 状态 |
|------|------|------|
| 2026-09-22 | 演义检索出现 `tool` 3.2 s（基线 60–100 ms），同一 trace 还多了一轮 generation；疑与检索冷启动 / 向量加载相关 | 待复现确认 |
| 2026-09-22 | 列表里 `domain` 空 + `server` 0–50 ms + 无 llm / tool 的行是 bug-00012 测试噪声；采基线、算分布前必须先剔除 | 已确认 |
| 2026-09-22 | 日志页「耗时」tooltip 的 6 项与主表时间点一一对应；工具「调用方」列是 `caller` + `stage` 合成（bug-00019 / bug-00020 后口径） | 已确认 |
| 2026-09-22 | `token-stats` 按天 / 按小时聚合的日界是 Asia/Shanghai，与页面展示时区一致 | 已确认 |
| 2026-09-22 | 排查接口自身不落日志（防递归）→ 日志页本身的问题不能靠日志排查 | 已确认 |
| 2026-09-22 | `/api/chat` 与 `/api/sango/random` 共用串行队列 → 并发测试时 `queueWait` 会互相影响，对照实验须串行发起 | 已确认 |
| 2026-09-23 | 演义域病句实测（aba8b6fe / 1a5fe278）：无任何「问句形态 / 置信」判定点，靠 fastpath 或分类轮 `1` 直进演义域 + 生成轮按片段作答答对；但 `finalScore` top10 全距仅 0.68–0.75（top1 0.75）——若未来加「低置信拒答」门限，演义域会批量误伤（bug-00003 同背景），拒答判据应保持「内容缺失」而非问句形态 | 已确认（实测） |

## 9. 使用约定

- 缺口编号 `GAP-XX` 自增、不复用；解决后从「日志采集缺口」移到「已解决的卡点」并写明解决方式
- 补采集属代码变更：按第 1 条走票（新 feat 或并入在做的特性），本文件只登记不派活
- 缺口只写「排查时需要什么数据 + 为什么现有数据不够」，实现方案留给接口文档
- 本文件是排查的「外挂记忆」：上下文压缩后靠它接续，不靠回忆
